[build]
  builder     = "NIXPACKS"
  buildCommand = "npm run build"

[deploy]
  startCommand         = "node server/index.js"
  healthcheckPath      = "/api/auth/hm-me"
  healthcheckTimeout   = 60
  restartPolicyType    = "ON_FAILURE"
  restartPolicyMaxRetries = 5
